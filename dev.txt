# Winning Blueprints — Dev Progress
# Updated: All core phases complete

## Done
[x] Phase 0–5: Scaffold, public site, auth, RBAC, academy, client, ops portals
[x] Phase 6: Sales lead pipeline
    - Lead status workflow (New → Contacted → Qualified → Proposal → Won/Lost)
    - Assign to sales executive (HTMX)
    - Handoff notes
    - Convert lead → ClientAccount + Project
    - Lead email notifications (console in dev)
[x] Phase 7: Billing MVP
    - Invoice model + admin
    - Client invoices view
    - Ops invoices view
    - seed_billing command
[x] Production readiness
    - Env-based settings (dev/prod)
    - Whitenoise + Gunicorn + PostgreSQL support
    - .env.example + README
    - Password reset flow
    - Automated tests (website, operations, billing)
[x] UI polish
    - Dashboard navy/gold theme aligned with public site
    - Home why-choose + featured case study

[x] Meta Ads landing page — AI Digital Marketing Course
    - /ai-digital-marketing-course/ — standalone conversion page, own minimal layout (no site nav/footer)
    - Backed by existing CourseListing 'digital-marketing-with-ai' — price stays editable only in /ops/courses/
    - Compact lead form (name/phone/email) posts to /ai-digital-marketing-course/lead/, reuses Lead model + notifications
    - Meta Pixel (PageView/ViewContent/Lead/InitiateCheckout) gated on META_PIXEL_ID env var — no-op until set
    - WhatsApp click-to-chat CTA, sticky mobile CTA bar, FAQ, testimonials, curriculum accordion
    - Buy Now CTA goes to existing Razorpay checkout flow (academy:checkout)
    - Upgrade: scroll-reveal animations, comparison table (free tutorials vs course), career salary
      bars (entry/senior), mentor bio section (real TEAM_CORE data), exit-intent lead popup, hero
      floating certificate badge, trust-icon rows — no fabricated guarantee/refund claims added
    - Fix: static/css/app.css was stale (built before this page's templates existed) — Tailwind
      purge scan skipped any utility class only used here, so several sections rendered unstyled/
      missing. Ran `npx tailwindcss -i static/src/input.css -o static/css/app.css --minify` to
      rebuild. Remember to rerun this (or `npm run build:css`) after adding new templates.
    - Also swapped all emoji trust/check/x icons (🔒🏆♾️🔥✓✕) for inline SVG — emoji don't take
      Tailwind text-color classes (silently ignored the gold/emerald tint) and render inconsistently
      across OS/browsers; bumped several text-sm → text-base + slate-400 → slate-300 on dark
      sections for readability
    - Split into two DISTINCT CourseListing rows (was wrongly sharing one row with the landing page
      — editing price/name for the ad page leaked onto the real academy catalog course):
        - 'digital-marketing-with-ai' — original, reverted: AI-Powered Digital Marketing Mastery,
          ₹16,999, 12 weeks, full curriculum. Untouched by the landing page from now on.
        - 'quick-start-digital-marketing' — new, landing-page-only: Quick Start Digital Marketing,
          ₹999, 4 weeks, own 4-module curriculum/gains/includes/career data (deliberately lighter
          tier than the ₹16,999 course — fewer live-support perks, honest value ladder). Also has
          its own /academy/courses/quick-start-digital-marketing/ page + checkout since CourseListing
          has no catalog-hidden flag — accepted as a real, separately purchasable course.
        - enrolled=0, rating=0, reviews=[] on the new course (honest — no fake social proof for a
          brand-new offer). Template (ai_digital_marketing.jinja) now branches on
          course.enrolled/course.reviews_count: shows real numbers when present, else "Just
          launched" / "Beginner friendly" / "Certified" fallbacks instead of "0 rated · 0 learners"
        - website/views.py LP_AI_DM_SLUG now points at 'quick-start-digital-marketing'
    - Removed the exit-intent popup modal (kept the inline enroll-section lead form). Added more
      SVG icons throughout: stats strip, career-path chips, testimonial quote marks, etc.
    - Upgraded /academy/courses/quick-start-digital-marketing/ (shared course_detail.jinja, same
      site theme — no template redesign). Course has no real students yet, so instead of fabricating
      fake student reviews: added an 'aggregate trust' fallback section (course_detail.jinja,
      academy/views.py) that shows when course.reviews is empty — real company-wide STATS +
      real mentor (TEAM_CORE) bio, honestly framed as 'just launched, be an early learner' rather
      than fake social proof. Original course (has real reviews) is unaffected — still shows its
      normal per-course review section. Also wrote fuller overview copy (3 paragraphs) and an
      honest mentor-authored quote (not a fake customer testimonial) for the new course.

[x] Ops course listing form (/ops/courses/<id>/edit/, /ops/courses/ add form) — readability upgrade
    - Widened scope to CourseListingForm only (academy/forms.py) — this form isn't shared with any
      other ops screen, so bumped .ops-select/.ops-textarea to text-base + py-2.5 + border-slate-300
      here without touching the shared classes used in tables/filters elsewhere in /ops/
    - Labels: text-xs text-slate-600 → text-sm font-semibold text-slate-800 (darker, bigger)
    - Field grids gap-3 → gap-4, form wrapper gap-4 → gap-6 for more breathing room
    - Verified with a temp superuser session (created + deleted after test) against
      /ops/courses/11/edit/ — 200, correct classes rendered, no template artifacts

[x] Performance audit + fixes
    - Root cause of "new course missing on prod": it was seeded via a one-off manage.py shell
      command against the local DB only. Added academy/migrations/0007_seed_quick_start_course.py
      (same RunPython/get_or_create pattern as 0006) so it deploys via the normal migrate step —
      run `docker compose exec web python manage.py migrate` on the server after pulling.
    - Infra layer was already solid: Redis cache+sessions, gzip, whitenoise CompressedManifest
      static storage, nginx static cache 30d/media 7d, Postgres CONN_MAX_AGE pooling, HTTP/2 at
      the host proxy. Checked DB query counts on hot pages (home, academy courses, course detail,
      landing page) — all 0-2 queries, no N+1 issue found.
    - Real gap found: almost no <img> tags used the existing partials/_img.jinja lazy-load
      component (only team/careers/about did). Added loading="lazy" decoding="async" to
      below-the-fold images across home, our_work, case_studies, case_study_detail, course cards,
      course_detail mentor photo, and the landing page mentor photo. Left true above-the-fold
      hero/LCP images (home hero visual, landing page course image, logo) eager, added
      fetchpriority="high" decoding="async" to the two hero images.
    - nginx.conf: added gzip_vary on; and more gzip_types (woff/ttf/ico/wasm were missing).

[x] Rebrand: Quick Start Digital Marketing -> Winning Blueprints Digital Launchpad
    - Same slug/URL/checkout ('quick-start-digital-marketing'), full content rewrite via
      academy/migrations/0009_rebrand_digital_launchpad.py (force update, same pattern as 0008)
    - New positioning: 4-skill live bootcamp — Vibe Coding (AI dev) + Meta Ads + Google Ads +
      Client Handling, 12 live classes over 4 weeks, ₹999 (was ₹2,999, shown struck-through on
      the landing page hero + enroll section — hardcoded campaign copy, not a DB field)
    - format changed 'Self-paced' -> 'Live online' to match the actual content (12 live days) —
      this also auto-hides the "Lifetime Access" badge on course_detail.jinja (made conditional
      on format) since that claim doesn't fit a live cohort and wasn't in the source content
    - Cleared career_label/career_min/career_max/salary_range — source content gives no salary
      figures, didn't fabricate any (auto-hides the career-outcomes section on both pages)
    - New curriculum: 4 modules / 54 topics, rewritten gains/includes/ideal_paths/overview
    - New FAQs (website/data.py LP_AI_DM_FAQS) — 8 Qs from the source content incl. the "we
      don't guarantee jobs/clients" disclaimer
    - highlight_quote/highlight_author switched from the single Marketing Expert mentor to
      'Winning Blueprints Academy' — content now spans coding + 2 ad platforms + sales, not just
      one person's specialty

[x] Perf fixes from live Lighthouse/PSI report
    - Razorpay checkout.js (~60KB+, "Legacy JavaScript"/"unused JS" in the report) was loaded
      sitewide via components/head_assets.jinja on every page (home, about, services, ...) even
      though `new Razorpay(...)` only ever runs on the academy checkout flow. Moved the script tag
      out of head_assets.jinja into templates/pages/academy/checkout.jinja's extra_head block —
      confirmed via grep it's the only page that needs it (partials/dashboard/_checkout_launch.jinja
      is only ever rendered from academy:enroll_pay, reached only from that checkout page).
    - nginx.conf /static/ cache bumped 30d -> 1y — safe because whitenoise's
      CompressedManifestStaticFilesStorage content-hashes filenames, so any content change already
      gets a new URL; no reason to under-cache.
    - website/data.py img() helper (used for all Unsplash-sourced images site-wide: hero, team,
      case studies, etc.) quality 70 -> 65 — meaningfully smaller payloads, still high visual
      quality, auto=format still lets Unsplash serve WebP/AVIF where supported.
    - Did NOT touch static/img/logo.png (explicitly asked not to compress it), despite it being
      the single largest flagged item (~118KB estimated savings) in the report.

[x] Public-site CMS in /ops/cms/
    - Every public page (home, services, about, team, case studies, pricing, startup, careers,
      industries, contact, the Digital Launchpad landing page) was 100% hardcoded in website/data.py
      (~30 Python constants, ~1000 lines) — zero DB, zero non-dev editability. Moved it into 11 DB
      models (website/models.py) grouped by shape, not by page, so it's 11 models instead of 30:
      TitleDescBlock (generic title/desc/icon/image/badge card, tagged by `section` — covers
      WHY_CHOOSE_US, STARTUP_PROMISES, STARTUP_FOR, F2C_PIPELINE, MODEL_STEPS, INDUSTRIES,
      TEAM_DEPARTMENTS, TEAM_ROLES, CAREERS_PERKS, ACADEMY_PROCESS, AUDIENCE_TAGS, HERO_POINTS),
      StatBlock, ProjectBlock, Testimonial, FAQ, TeamMember (unifies old TEAM_FOUNDERS + TEAM_CORE
      via is_founder), Service (also absorbs FLAGSHIP_OFFERS via is_flagship), CaseStudy,
      PricingTier, StartupPhase, SiteSettings (singleton — site images + contact info).
    - website/migrations/0010_cms_models.py (schema) + 0011_seed_cms_content.py (RunPython
      get_or_create, copies today's data.py content verbatim — row counts verified to match
      exactly: 55/4/5/3/23/10/8/4/3/5/1).
    - CLIENT_LOGOS and INSTAGRAM_IMAGES left out — dead code, not referenced by any view or
      template. LP_AI_DM_COMPARISON left hardcoded — single campaign-page comparison table,
      matches the existing precedent of not DB-driving one-page campaign copy.
    - Editor: one generic registry-driven CRUD (operations/cms_registry.py + cms_views.py +
      cms_forms.py) instead of 11 hand-written view/template sets — mirrors the existing
      CourseListingsView/CourseListingEditView pattern but resolves model/form dynamically via a
      `<slug:model_key>` URL segment. List/JSON fields (tags, gallery, features, outcomes...) edit
      as one-per-line textareas, same convention as CourseListingForm — helpers now shared via
      core/form_utils.py (academy/forms.py updated to import from there instead of duplicating).
      New nav entry: Website Content, superuser-only, at /ops/cms/.
    - website/content.py — query helpers backing the public pages. Most models reuse the exact
      field names data.py already had (services, case studies, pricing tiers, startup phases,
      stats, projects, testimonials, team members all pass straight through). Only FAQ (q/a vs
      question/answer) and the two TEAM_ROLES/TEAM_DEPARTMENTS sections (role/count vs title/badge)
      needed a dict-mapping shim — kept in content.py so templates needed ZERO changes.
    - website/views.py, academy/views.py, website/context_processors.py rewired from data.py
      imports to content.py queries, keeping identical context variable names/shapes throughout.
      SiteSettings cached 5 min via Redis (website/content.py:site_settings) to avoid a query on
      every page load for rarely-changing contact/hero image data.
    - Verified: manage.py check clean; all 17 public URLs curled — 200, zero unrendered {{ }}
      artifacts; spot-checked the two shimmed sections (team department/role counts, FAQ text)
      rendered correctly; full CRUD (list/add/edit/toggle/delete) exercised against all 11 model
      types via a temp superuser session, then deleted. Pre-existing test suite has 20 unrelated
      failures (Python 3.14 / installed Django's test client `copy.copy()` on RenderContext,
      confirmed via git stash to fail identically on unmodified main — not caused by this change).

[x] Team CMS — image upload
    - TeamMember.image_upload (FileField, upload_to='team/') added alongside the existing image
      URL field — website/migrations/0012_team_member_image_upload.py, purely additive.
    - Plain FileField not ImageField — Pillow isn't a project dependency and this doesn't need
      server-side image validation, just storage; avoids adding a new dependency for one field.
    - TeamMemberForm.save(): if a file is uploaded, `image` is overwritten with the upload's
      /media/ URL, so the public team.jinja/home.jinja templates (which only read `member.image`)
      need zero changes — upload just becomes another way to set that same field. Leaving the
      Image URL field blank/untouched still works exactly as before.
    - operations/cms_views.py now passes request.FILES through on all 3 CMS POST paths (add/edit/
      singleton-save); the 3 generic CMS <form> tags got enctype="multipart/form-data" — both
      changes apply to all 11 CMS content types (harmless no-op for the ones with no file field),
      not just Team.
    - Verified end-to-end against a temp superuser session: uploaded a real file, confirmed it
      landed in media/team/, confirmed `image` auto-synced to the /media/ URL, then restored the
      test row's original data and deleted the test file/user.
    - docker-compose.yml already mounts media_data as a persistent named volume (confirmed before
      building this) — uploads survive `docker compose up --build` redeploys.

## Ops Portal Modules (/ops/)
- /ops/                       Overview + stats
- /ops/quality-check/         Approve/reject deliverables
- /ops/projects/              Project staffing validation
- /ops/pipeline/              Talent pipeline
- /ops/allocation/            Staff/student assignment
- /ops/mentors/               Mentor allocation
- /ops/team/                  Team org
- /ops/performance/           Performance reviews
- /ops/leads/                 Lead pipeline (HTMX)
- /ops/invoices/              Invoice overview
- /ops/job-applications/      Careers applications, filters, dedupe guard
- /ops/job-openings/          Manage /careers/ roles + approve external company postings
- /careers/post-a-job/        Public form for outside companies to submit a role (pending review)

## Seed Data
python manage.py seed_users
python manage.py seed_academy
python manage.py seed_clients
python manage.py seed_ops
python manage.py seed_billing

## Test Logins
See test_user.txt

## Run
source venv/bin/activate
python durga.py

## Optional Next
[ ] Phase 8: Analytics exports, advanced ROI dashboards
[ ] Payment gateway (Razorpay/Stripe)
[ ] Blog/CMS
[ ] CI/CD

## Production Deploy
See deploy/DEPLOY.md and .env.prod
docker compose --env-file .env.prod up -d --build
Live: https://winningblueprints.com/ | https://159.195.52.197/
