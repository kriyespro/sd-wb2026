{% extends "base.jinja" %}

{% block content %}
<section class="relative flex min-h-[80vh] items-center overflow-hidden bg-ink-900 py-16">
    <div class="mesh absolute inset-0 opacity-80"></div>
    <div class="relative mx-auto w-full max-w-md px-4">
        <div class="rounded-3xl border border-white/10 bg-white p-8 shadow-2xl">
            <h1 class="font-display text-2xl font-bold text-ink-900">Set new password</h1>
            <form method="post" class="mt-8 space-y-4">
                <input type="hidden" name="csrfmiddlewaretoken" value="{{ csrf_token }}">
                {% if form.non_field_errors() %}
                <div class="rounded-lg bg-red-50 p-3 text-sm text-red-700">{{ form.non_field_errors()[0] }}</div>
                {% endif %}
                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700">New password</label>
                    {{ form.new_password1 }}
                </div>
                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700">Confirm password</label>
                    {{ form.new_password2 }}
                </div>
                <button type="submit" class="w-full rounded-full bg-brand-600 px-4 py-3 text-sm font-semibold text-white hover:bg-brand-500">Update password</button>
            </form>
        </div>
    </div>
</section>
{% endblock %}
